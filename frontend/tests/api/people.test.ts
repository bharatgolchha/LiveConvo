import { describe, it, expect } from 'vitest';

describe('People API', () => {
  it('list endpoint exists', async () => {
    expect(true).toBe(true);
  });

  it('detail endpoint exists', async () => {
    expect(true).toBe(true);
  });
});


